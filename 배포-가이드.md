# 연구실 웹페이지 배포 및 관리 가이드

al-folio 테마 기반 Jekyll 사이트입니다. **코딩은 필요 없습니다.** 텍스트 파일(`.md`, `.yml`) 내용만 고치면 됩니다.

---

## 1. 최초 배포 (약 20분)

### 1-1. GitHub 계정 및 저장소 생성

1. <https://github.com> 가입 (이미 있으면 생략)
2. 우측 상단 `+` → **New repository**
3. **Repository name** 입력 — 아래 세 방식 중 선택 (다음 절 참고)
4. **Public** 선택 (Private 은 GitHub Pages 무료 호스팅 불가)
5. **Create repository**

#### 주소를 어떻게 정할 것인가

GitHub 아이디를 반드시 써야 하는 것은 아닙니다. 세 가지 방식이 있습니다.

| 방식 | 저장소 이름 | 최종 주소 | `baseurl` 설정 |
| --- | --- | --- | --- |
| ① 개인 사이트 | `아이디.github.io` | `https://아이디.github.io` | `""` (빈칸) |
| ② 프로젝트 사이트 | 자유 (예: `ldpl`) | `https://아이디.github.io/ldpl` | `"/ldpl"` |
| ③ 조직(Organization) | `조직명.github.io` | `https://조직명.github.io` | `""` (빈칸) |

**① 개인 사이트** — 저장소 이름이 반드시 `아이디.github.io` 와 정확히 일치해야 합니다. 아이디 부분을 임의로 바꿀 수 없습니다. 계정당 하나만 만들 수 있습니다.

**② 프로젝트 사이트** — 저장소 이름은 자유입니다. 다만 주소에 아이디가 남습니다(`아이디.github.io/ldpl`). 대신 계정당 개수 제한이 없어 강의 페이지 등을 따로 만들 수 있습니다.

**③ 조직 계정 (연구실 이름을 주소에 넣고 싶을 때 권장)** — 아이디와 무관한 이름을 쓸 수 있습니다.

1. GitHub 우측 상단 `+` → **New organization** → Free 플랜 선택
2. 조직 이름을 연구실 약칭으로 입력 (예: `scnu-ldpl`)
3. 그 조직 안에 `scnu-ldpl.github.io` 저장소 생성
4. 주소는 `https://scnu-ldpl.github.io`

조직 계정은 무료이고, 나중에 학생을 공동 관리자로 추가하거나 담당자를 인계하기 쉽습니다. 개인 아이디에 종속되지 않으므로 연구실 사이트에는 이 방식이 적합합니다.

> 어느 방식이든 **커스텀 도메인**(예: `ldpl.scnu.ac.kr`)을 연결하면 GitHub 주소는 겉으로 드러나지 않습니다. 6절 참고.

### 1-2. 파일 업로드

**먼저 `연구실-웹페이지.zip` 의 압축을 풀어야 합니다.** zip 파일 자체를 올리면 사이트가 만들어지지 않습니다.

압축을 풀면 `lab-website` 폴더가 생기고, 그 안에 `_config.yml`, `_pages`, `assets` 등이 들어 있습니다.

> **중요: `lab-website` 폴더 자체가 아니라, 그 폴더 *안의* 내용물을 올려야 합니다.**
> 저장소 최상단에 `_config.yml` 이 바로 보여야 정상입니다.
> `lab-website/_config.yml` 처럼 한 단계 들어가 있으면 빌드가 실패합니다.

1. 만든 저장소 화면에서 **uploading an existing file** 클릭
2. `lab-website` 폴더를 연 뒤, 그 안의 **모든 파일과 폴더를 전체 선택**(Ctrl+A / Cmd+A)해서 드래그
   - 폴더째 드래그하면 하위 파일까지 함께 올라갑니다
   - 이름이 `.` 으로 시작하는 폴더(`.github`)가 빠지지 않았는지 확인 — 이게 없으면 배포가 안 됩니다
3. 아래 **Commit changes** 클릭

**`배포-가이드.md` 는 올려도 되고 안 올려도 됩니다.** 사이트에는 표시되지 않으므로 무해하며, 함께 올려두면 나중에 GitHub에서 바로 열어볼 수 있어 편리합니다. 실제로 압축 파일 안에도 같은 가이드가 들어 있습니다.

> **`.github` 폴더가 안 보이거나 드래그되지 않는 경우**
>
> - Windows 탐색기: 보기 → **숨긴 항목** 체크
> - macOS Finder: `Cmd + Shift + .` (마침표)
>
> 그래도 안 올라가면 저장소 화면에서 `Add file → Create new file` 을 누르고 파일명 칸에
> `.github/workflows/deploy.yml` 을 입력한 뒤, 압축 파일 안의 같은 파일 내용을 붙여넣으십시오.
> 슬래시(`/`)를 입력하면 폴더가 자동으로 만들어집니다.

업로드가 끝나면 저장소 최상단이 아래와 같아야 합니다.

```
.github/    _bibliography/  _config.yml  _data/     _news/
_pages/     _projects/      assets/      Gemfile    package.json  ...
```

### 1-3. 설정 수정

저장소에서 `_config.yml` 파일을 클릭 → 연필 아이콘(Edit) → 아래 두 줄 수정:

```yaml
url: https://scnu-ldpl.github.io   # 위 표에서 정한 주소의 도메인 부분
baseurl: ""                        # ①③ 방식이면 빈칸, ② 방식이면 "/저장소명"
```

`url` 은 `https://` 부터 `.github.io` 까지만 적고, 뒤의 경로는 `baseurl` 에 적습니다.
예를 들어 ② 방식으로 `hclee.github.io/ldpl` 을 쓴다면 `url: https://hclee.github.io`, `baseurl: "/ldpl"` 입니다.

수정 후 **Commit changes**.

### 1-4. GitHub Pages 켜기

1. 저장소 상단 **Settings** → 좌측 **Pages**
2. **Source** 를 `Deploy from a branch` 로 두고
3. **Branch** 를 `gh-pages` / `/ (root)` 로 선택 → **Save**
   - `gh-pages` 브랜치는 첫 빌드가 끝나야 생깁니다. 목록에 없으면 5분 뒤 다시 확인
4. 저장소 상단 **Actions** 탭에서 빌드 진행 상황 확인 (초록 체크 = 성공)

빌드는 약 3~5분 걸립니다. 완료되면 1-1절 표에서 정한 주소로 접속됩니다.

### 1-5. 배포 권한 확인 (실패 시)

Actions 탭에서 빨간 X 가 뜨면:

- **Settings → Actions → General → Workflow permissions** 에서
  `Read and write permissions` 선택 후 Save
- Actions 탭에서 실패한 워크플로우 → **Re-run all jobs**

---

## 2. 내용 수정 방법

### 웹 브라우저에서 직접 수정 (가장 간단)

1. GitHub 저장소에서 고칠 파일 클릭
2. 연필 아이콘 → 내용 수정 → **Commit changes**
3. 3~5분 뒤 사이트에 반영

### 무엇을 어디서 고치는가

| 하고 싶은 일 | 파일 |
| --- | --- |
| 연구실 이름, 사이트 주소, 언어 설정 | `_config.yml` |
| 첫 화면 소개글, 프로필 사진, 사무실 주소 | `_pages/about.md` |
| 논문 추가 | `_bibliography/papers.bib` |
| 연구분야 카드 추가/수정 | `_projects/` 폴더의 `.md` 파일 |
| 구성원 명단 | `_pages/people.md` |
| 실험장비 목록 | `_pages/facilities.md` |
| 공지·소식 | `_news/` 폴더에 새 파일 생성 |
| 이력(학력·경력·수상) | `_data/cv.yml` |
| 이메일, Google Scholar 링크 | `_data/socials.yml` |
| 논문 저널 배지 색상 | `_data/venues.yml` |

### 사진 교체

| 위치 | 파일 경로 |
| --- | --- |
| 교수 프로필 | `assets/img/prof_pic.jpg` |
| 연구분야 카드 | `assets/img/research/1.jpg` ~ `4.jpg` |
| 논문 미리보기 | `assets/img/publication_preview/` |

**같은 파일 이름으로 덮어쓰면** 코드를 고칠 필요가 없습니다.
GitHub에서 해당 폴더로 들어가 `Add file → Upload files` 로 같은 이름의 파일을 올리면 교체됩니다.
현재 들어 있는 회색 이미지는 모두 자리표시용입니다.

---

## 3. 논문 목록 관리 (가장 자주 쓰는 기능)

`_bibliography/papers.bib` 파일 하나만 관리하면 논문 페이지가 자동 생성됩니다.

### 논문 추가 절차

1. Google Scholar에서 본인 논문 검색
2. 논문 아래 **인용(따옴표) 아이콘** 클릭 → 팝업 하단 **BibTeX** 클릭
3. 나오는 텍스트 전체 복사
4. `papers.bib` 파일 편집 화면에 붙여넣기 → Commit

### 유용한 추가 필드

기본 BibTeX 항목 안에 아래 줄을 넣으면 기능이 추가됩니다.

```bibtex
@article{lee2026something,
  title       = {논문 제목},
  author      = {Lee, Hyunchang and Hong, Gildong},
  journal     = {Journal of Propulsion and Power},
  year        = {2026},
  selected    = {true},              % 첫 페이지 대표논문에 표시
  bibtex_show = {true},              % BibTeX 보기 버튼
  abbr        = {JPP},               % 왼쪽 저널 배지
  doi         = {10.2514/1.B00000},  % DOI 링크
  pdf         = {mypaper.pdf},       % assets/pdf/ 에 올린 파일명
  preview     = {mypaper.png}        % 미리보기 이미지
}
```

### 저자 이름 굵게 표시

`_config.yml` 의 아래 부분에 본인 이름 표기를 모두 넣어두면 목록에서 굵게 표시됩니다.

```yaml
scholar:
  last_name: [Lee, 이]
  first_name: [Hyunchang, H.C., H., 현창]
```

---

## 4. 새 소식 올리기

`_news/` 폴더에 `연도-월-일-제목.md` 형식으로 파일을 만듭니다.

**짧은 한 줄 공지**

```markdown
---
layout: post
date: 2026-09-01 09:00:00+0900
inline: true
related_posts: false
---

○○○ 학생이 한국추진공학회 추계학술대회에서 우수논문상을 수상했습니다.
```

**본문이 있는 공지**

```markdown
---
layout: post
title: 신규 장비 도입
date: 2026-09-01 09:00:00+0900
inline: false
related_posts: false
---

본문을 여기에 작성합니다. 이미지, 표, 수식 모두 사용 가능합니다.
```

`+0900` 은 한국 표준시입니다. `inline: true` 는 첫 화면에 한 줄로, `false` 는 별도 페이지로 표시됩니다.

---

## 5. 연구분야 항목 추가

`_projects/` 폴더에 새 `.md` 파일을 만듭니다.

```markdown
---
layout: page
title: 초임계 조건 분무 특성
description: 초임계 압력에서의 제트 거동 계측
img: assets/img/research/5.jpg
importance: 5
category: 계측기법
---

본문 내용을 작성합니다.
```

- `importance` 숫자가 작을수록 앞에 표시됩니다
- `img` 에 지정한 이미지는 미리 해당 경로에 올려두어야 합니다

---

## 6. 커스텀 도메인 연결 (선택)

학과 도메인이나 개인 도메인을 쓰려면:

1. 저장소 루트에 `CNAME` 파일을 만들고 도메인만 한 줄 입력 (예: `ldpl.scnu.ac.kr`)
2. 도메인 관리자(학교 전산팀)에게 DNS CNAME 레코드를 `계정명.github.io` 로 지정 요청
3. Settings → Pages → Custom domain 에 도메인 입력, **Enforce HTTPS** 체크

---

## 7. 로컬에서 미리보기 (선택, 필수 아님)

수정 결과를 올리기 전에 컴퓨터에서 확인하고 싶을 때만 필요합니다.
가장 간단한 방법은 Docker 입니다.

```bash
docker compose up
```

이후 브라우저에서 <http://localhost:8080> 접속. 파일을 수정하면 자동으로 반영됩니다.

Docker 없이 하려면 Ruby 3.x 설치 후:

```bash
bundle install
bundle exec jekyll serve
```

---

## 8. 자주 생기는 문제

| 증상 | 원인 및 해결 |
| --- | --- |
| Actions 탭에 아무것도 안 뜸 | `.github/workflows/deploy.yml` 이 업로드되지 않았음. 1-2절의 숨김 폴더 안내 참고 |
| 빌드는 되는데 사이트가 비어 있음 | 파일이 `lab-website/` 하위에 올라갔을 가능성. 저장소 최상단에 `_config.yml` 이 바로 보여야 함 |
| Actions에서 빌드 실패 (빨간 X) | 대부분 `.yml` 또는 `.md` 파일의 앞부분(`---` 사이) 문법 오류. 들여쓰기와 콜론 뒤 공백 확인 |
| 사이트가 안 열림 | Settings → Pages 의 Branch 가 `gh-pages` 인지 확인. 첫 빌드 완료 전에는 브랜치가 없음 |
| CSS가 깨져 보임 | `_config.yml` 의 `url` 과 `baseurl` 이 실제 주소와 다름 |
| 논문이 안 보임 | `papers.bib` 의 중괄호 `{ }` 개수가 안 맞음. 붙여넣은 항목의 마지막 `}` 확인 |
| 한글이 깨짐 | 파일을 UTF-8 로 저장. GitHub 웹 편집기를 쓰면 자동으로 UTF-8 |
| 이미지가 안 나옴 | 파일명 대소문자 확인 (`Photo.JPG` 와 `photo.jpg` 는 다른 파일로 취급됨) |

---

## 9. 참고 링크

- al-folio 공식 저장소: <https://github.com/alshedivat/al-folio>
- al-folio 데모 사이트: <https://alshedivat.github.io/al-folio>
- 커스터마이징 문서: <https://github.com/alshedivat/al-folio/blob/main/docs/CUSTOMIZE.md>
- 자주 묻는 질문: <https://github.com/alshedivat/al-folio/blob/main/docs/FAQ.md>
- Markdown 문법 요약: <https://www.markdownguide.org/cheat-sheet/>
